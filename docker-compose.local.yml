services:
  backend:
    ports:
      - "5101:8081"

  frontend:
    build:
      args:
        VITE_API_BASE: ${LOCAL_API_BASE:-http://localhost:5101/api}
        VITE_WS_URL: ${LOCAL_WS_URL:-ws://localhost:5101/ws}
        VITE_WS_PATH: /ws
    ports:
      - "5100:8080"

